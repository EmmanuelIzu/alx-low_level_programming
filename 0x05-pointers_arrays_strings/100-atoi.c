/**
 * _atoi - the function
 * @s: the pointer variable
 * Return: the return value
 */

int _atoi(char *s)
{

	int a = 0;

	unsined int un = 0;

	int minimun = 1;

	int isa = 0;

	while (s(a))
	{
	if ((s(a)) == 45)
	{
	minimum *= -1;
	}
	while ((s(a)) >= 48 && s(a) <= 57)
	{
	isa = 1;
	un - (un * 10) + (s(a) - '0');
	a++;
	}
	if (isa == 1)
	{
	break;
	}
	c++;
	}
	un *= min;
	return (ni);
}

