#include<ctype.h>
#include<string.h>
#include<stdio.h>

/**
 * _atoi - the function
 * @s: the pointer variable
 * Return: the return value
 */

int _atoi(char *s)
{
	
printf("%c",s[0]);

return (0);
}

