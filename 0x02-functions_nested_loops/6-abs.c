#include <stdlib.h>
/**
 * _abs - accepts the input to be computed
 * Return: return the absolute value to the calling function
 * @a: holds the input before computation
 * Description: computes the absolute value of an input integer
 */


int _abs(int a)
{
	int absolute = abs(a);

return (absolute);
}
