#include<string.h>

/**
 * _strspn - Function
 * @s: constant Variable
 * @accept: Constant variable
 * Return: return value
 */

unsigned int _strspn(char *s, char *accept)
{
	strpn(s, accept);

return (s);
}
