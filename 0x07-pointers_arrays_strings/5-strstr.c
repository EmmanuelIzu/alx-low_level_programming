#include<string.h>
/**
 * _strstr - The main function
 * @haystack: input variable
 * @needle: input variable
 * Return: return a pointer
 */

char *_strstr(char *haystack, char *needle)
{
	char *begin = strstr(haystack, needle);


return (begin);
}
